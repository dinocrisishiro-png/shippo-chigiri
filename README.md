# ググガガ しっぽちぎり

ペンギン「ググガガ」のしっぽをちぎる 60 秒スコアアタックゲーム。
`index.html`（Vanilla JS / Canvas、外部ライブラリなし）と `assets/` のスプライト PNG で動きます。

## 遊び方

1. ググガガに**そーっと**近づく（ポインタを速く動かして近づくと「！」で気づかれて逃走される）
2. しっぽをタップ / クリックで掴む（マウス・タッチ両対応）
3. ググガガが逃げる方向と**逆**にドラッグして張力ゲージをためる
4. ゲージが満タンになると「ちぎり！」でスコアゲット（連続でちぎるとコンボ倍率）
5. ちぎられたググガガはフェードアウトして消え、新しいググガガがスポーンしてくる。60 秒間でハイスコアを目指そう

画面には常に 1〜3 体のググガガがいて（2〜5 秒間隔でスポーン）、色によって点数と寿命が違います。

| 色 | 出現率 | 点数 | 寿命 |
|---|---|---|---|
| 黒 | 60% | 100 | 消えない |
| 赤 | 25% | 300 | 8 秒 |
| 青 | 12% | 500 | 6 秒 |
| 白 | 3% | 2000 | 4 秒 |

色付きは消滅 1.5 秒前から点滅して警告し、時間切れでフェードアウトします。ベストスコアは `localStorage` に保存されます。

## ググガガのステートマシン

```
のんびり ──(ポインタが速く接近)──▶ 警戒(！) ──▶ 逃走
   ▲                                            │
   └────────(ほとぼりが冷める)───────────────────┘
のんびり/逃走中 ──(しっぽを掴む)──▶ 捕獲 ──(ちぎり！/すっぽ抜け)──▶ 逃走
```

## 実行

`index.html` をブラウザで開くだけ。ビルド不要。

## スプライト描画

- `assets/gugugaga_idle.png`（正面向き・全ステート共通）と `assets/tail.png` を `drawImage` で描画
- 左向き移動時は左右ミラー、警戒ステートは「！」オーバーレイで表現
- しっぽの付け根は体スプライトのお尻位置（`SPRITE_DEFS.idle.butt`）、掴み判定はしっぽ画像の先端（`SPRITE_DEFS.tail.grab`）の相対座標アンカーで定義
- `assets/tail.png` は本体画像の黒い生地部分を切り出してティアドロップ型に加工したもの
- 画像のロードに失敗した場合は従来の図形描画に自動フォールバック

## 更新履歴

### v0.5

- しっぽをちぎられたググガガはしっぽ再生ではなくフェードアウトして退場する仕様に変更（スポナーが新しい個体を補充）

### v0.4

- カラーバリアントシステム（黒 60%・赤 25%・青 12%・白 3%、色別の点数と寿命）
- 常時 1〜3 体の同時スポーン、消滅前の点滅警告とフェードアウト、白出現時のキラッとエフェクト
- 色調変換スプライトは起動時にオフスクリーン Canvas で事前生成（毎フレーム filter なし）
- 画面左下に点数凡例を常時表示

### v0.3

- 体スプライトを実写風の透過PNG（背景除去済み写真、高さ768px）に差し替え
- しっぽは本体画像の生地質感を切り出して合成、警戒スプライトは「！」オーバーレイに統合

### v0.2

- 描画をスプライト方式（透過 PNG + drawImage）に差し替え、図形描画はフォールバックに

### v0.1

- ステートマシン（のんびり → 警戒 → 逃走 → 捕獲）
- Pointer Events によるマウス / タッチ統一操作
- 張力ゲージ・ちぎり演出・コンボスコア
- しっぽ再生、60 秒スコアアタック、ベストスコア保存
